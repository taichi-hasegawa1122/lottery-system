# UTF-8エンコーディング設定
AddDefaultCharset UTF-8

# PHPエラー表示（本番環境では off にすることを推奨）
php_flag display_errors on
php_value error_reporting E_ALL

# セキュリティ設定：dataディレクトリへの直接アクセスを禁止
<Files "results.json">
    Order allow,deny
    Deny from all
</Files>

